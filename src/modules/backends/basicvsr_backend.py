def load_model(device: str):
    raise NotImplementedError(
        "BasicVSR++ is currently unavailable/deferred due to MMCV compilation dependency mismatches on Windows. "
        "Refer to resuult/validation_results.md for details."
    )

def infer_sequence(frame_window: list, device: str, scale: int = 4):
    raise NotImplementedError(
        "BasicVSR++ is currently unavailable/deferred due to MMCV compilation dependency mismatches on Windows. "
        "Refer to resuult/validation_results.md for details."
    )
